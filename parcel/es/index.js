import React from 'react'
import { render } from 'react-dom'
import App from './app'

class AppContainer extends React.components {
  state = {
    name: 'Parcel 打包案例'
  }

  componentDidMount () {
    setTimeout(() => this.setState({
      
    }))
  }
}